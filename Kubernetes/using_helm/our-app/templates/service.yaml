apiVersion: v1
kind: Service
metadata:
  name: {{ include "our-app.fullname" . }}
  labels:
    {{- include "our-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "our-app.selectorLabels" . | nindent 4 }}
