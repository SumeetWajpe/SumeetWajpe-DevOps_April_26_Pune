apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "our-app.fullname" . }}-test-connection"
  labels:
    {{- include "our-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "our-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
